# Implementation Progress Tracker

Use this file to track real-world progress standing up the JSIG maintenance program described in `MAINTENANCE-PLAN.md`. Keep the plan itself (`MAINTENANCE-PLAN.md`) stable as the reference; log actual status, dates, and owners here.

Last updated: 2026-08-06 (per-host output folder + 12-item hardening batch: dropped Notes/re-hash text, dropped Prior-Win10-Upgrade field, withheld domain name on 0.J, real disk-vendor inference, DCN-echo hardening, launch-time Examiner Initials prefill, alphabetized services list, Splunk-forwarder-aware audit-retention applicability, McAfee/Defender-aware Anti-Virus check, friendlier BIOS-vendor-module error text, and a real ad-group-map.example.json template)

## Phase status (mirrors MAINTENANCE-PLAN.md §6 Getting-Started Sequencing)

| Phase | Description | Status | Owner | Notes |
|---|---|---|---|---|
| 0 | Roles appointed in writing (ISSM, ISSO, SA, NA, SCA, FSO, Privacy Officer, Training Manager) | Not started | | |
| 1 | Baseline configuration & account management stood up (CM-2/CM-6, AC-2) | Not started | | |
| 2 | Audit logging & retention operational (AU family) | Not started | | |
| 3 | Vulnerability/patch management cadence operational (SI-2, RA-5) | Not started | | |
| 4 | Contingency & incident response plans tested (CP, IR) | Not started | | |
| 5 | Independent assessment / continuous monitoring cadence established (CA family) | Not started | | |
| 6 | Full 110-task master calendar operational across all 26 families | Not started | | |

## Reference-completeness tracker (mirrors EXTRACTION-LOG.md / EXTRACTION-LIMITATIONS.md)

| Item | Status | Notes |
|---|---|---|
| JSIG §1.5 Roles & Responsibilities | Extracted verbatim | See `reference/JSIG-source/section-1.5-roles-and-responsibilities.md` |
| JSIG Chapter 3: all 18 families (AC, AT, AU, CA, CM, CP, IA, IR, MA, MP, PE, PL, PS, RA, SA, SC, SI, PM) | Extracted verbatim, full re-extraction 2026-07-17 | See `reference/JSIG-source/chapter-3-*-family.md`; superseded prior partial/NIST-boilerplate versions after full PDF access was obtained via Wayback Machine + `pdftotext -layout` — see `EXTRACTION-LOG.md` |
| JSIG Privacy chapter: all 8 sub-families (AP, AR, DI, DM, IP, SE, TR, UL) | Extracted verbatim, full re-extraction 2026-07-17 | See `reference/JSIG-source/chapter-3-privacy-*-family.md`; previously used NIST 800-53 Rev.4 baseline proxy text only |
| Appendix C SAP Security Control Baselines | Extracted verbatim, full 963-row table, 2026-07-17 | See `reference/JSIG-source/appendix-c-sap-security-control-baselines.md`; dedicated column-position parser with spot-check verification gate; supersedes and replaces the removed `appendix-c-baselines-partial.md` |
| Appendix A References | Extracted verbatim | See `reference/JSIG-source/appendix-a-references.md` |
| Appendix B Acronyms | Extracted verbatim, re-extracted 2026-07-17 for layout-mode fidelity | |
| Appendix D Confidentiality Impact Level Flowchart | Genuine limitation, documented | Flowchart is a rendered image in the source PDF, not extractable text; see `EXTRACTION-LIMITATIONS.md` |
| Appendix E Definitions | Extracted verbatim, re-extracted 2026-07-17 for layout-mode fidelity | |
| Section 2 RMF Process | Extracted (mostly complete) | See `reference/JSIG-source/section-2-rmf-process.md`; some 2.4–2.7 continuation may be incomplete |

## Open questions / decisions needed

- [ ] Confirm which GRC tool, ticketing system, or calendar will operationalize the 110-task master calendar (`MAINTENANCE-PLAN.md` §4).
- [ ] Reconcile `MAINTENANCE-PLAN.md`'s 110-task master calendar against the real JSIG ODP values now available in `reference/JSIG-source/chapter-3-*-family.md` (e.g. AU-1/AT-1 "at least annually" is now confirmed verbatim) — not yet started; likely a separate phase since it may ripple into `RACI-MATRIX.md`, `role_task_index.json`, and the 17 role runbooks that cite specific ODP values. Scope with the user before starting.
- [ ] Assign real named owners to each role in `playbooks/roles/` (currently generic role titles only).

## Change log

- 2026-08-06 — Per-host output folder plus a 12-item hardening/correctness batch across the ISV toolchain, requested as one combined batch. **Output layout:** all collector/renderer output (evidence JSON, main report, BIOS guides) now lands in a folder named after the sanitized hostname (or `SAMPLE`/`UNKNOWN-HOST` fallback) instead of the flat `execution-plan/initial-system-validation/` directory; `evidence.json` is now `evidence-<hostname>.json` and the header of the rendered report reads that actual filename instead of a hardcoded `evidence.json` string (`New-MainReportHtml` gained an `-EvidenceFileName` param). **12-item batch:** (1) evidence filename fix, covered above; (2) removed the free-text `ReportNotes`/"Notes: CSU is an undefined..." block and its "An auditor can independently re-hash..." paragraph from the rendered report entirely (the re-hash explanation was preserved in `README.md`'s Tamper-evidence digest section instead); (3) removed the `PriorWin10Upgrade` identification field/prompt/param entirely — replaced by the Examiner Initials field, see (6); (4) `Get-DiskInventory` now runs disk `Model` strings through a new `Resolve-DiskMake` helper (~24 known vendor-prefix patterns: Samsung, WDC, Seagate/ST, Crucial/CT, SanDisk, Kingston, Toshiba, Hitachi/HGST, Intel, SK hynix, Micron, ADATA, Corsair, PNY, Transcend, Lexar, Fujitsu, LITEON, KIOXIA, etc.) instead of showing WMI's generic `"(Standard disk drives)"` class-name placeholder as if it were the manufacturer; falls back to the raw WMI Manufacturer value or `"Unknown (see Model)"` if no prefix matches; (5) hardened (not root-caused — not reproducible without a live Windows host) the reported non-functional per-drive DCN prompt by adding an explicit console echo (`-> Recorded DCN for drive N: <value>`) immediately after each prompt, including the generic single-drive fallback, so the operator gets immediate confirmation of what was captured; (6) added a single launch-time `-ExaminerInitials` prompt (via new `Read-IsvExaminerInitials` helper, which returns a genuinely empty string rather than the `"N/A -- not provided by operator"` placeholder other fields use when left blank) that pre-fills all 41 per-item Examiner Initials `<input>` fields in the rendered report — each field remains individually editable afterward for per-item sign-off; this is an interpretation of the request as one overall launch-time prompt rather than 41 individual per-item prompts, not separately confirmed with the user; (7) rewrote item `0.J` so it no longer captures or renders the actual domain/workgroup name — it now records only `"Membership status: Domain (domain name withheld from this report)"` or the Workgroup equivalent, based on `Win32_ComputerSystem.PartOfDomain`; (8) replaced the five per-vendor BIOS-check error throws — previously the raw exception-style text `"ERROR executing check: Neither DellBIOSProvider nor HP.ClientManagement module is installed on this system."` — with a friendlier, action-oriented message (`"No BIOS vendor module detected (DellBIOSProvider / HP.ClientManagement) -- use the manual fallback in the companion BIOS Reference Guide."`), and generalized the same tone change (`"Automated check unavailable -- ..."` instead of `"ERROR executing check: ..."`) to the generic per-item error-catch prefix used by all 41 checks; (9) alphabetized the 45-service list in item `2.A` (services.msc equivalent) so it's actually easy for a human reviewer to scan/cross-check; (10) item `4.D` (audit retention) now checks for an installed and actively-reporting Splunk Universal Forwarder (`Get-Service -Name SplunkForwarder` plus `Get-NetTCPConnection -State Established` on TCP 9997, the default Splunk forwarder-to-indexer data channel) to inform applicability, rather than assuming one branch or the other; (11) rewrote the Anti-Virus check to try a Trellix/McAfee legacy registry path (`HKLM:\SOFTWARE\WOW6432Node\McAfee\AVEngine`) first, falling back to Windows Defender (`Get-MpComputerStatus`) if absent, with an honest N/A (rather than a false PASS/FAIL) and an explanatory note when neither source is available — modern ePO-managed McAfee/Trellix ENS reportedly does not expose the DAT date locally at all in most configurations ([Reddit r/sysadmin](https://www.reddit.com/r/sysadmin/comments/816c51/mcafee_endpoint_security_platform_dat_date/), [Microsoft Learn Get-MpComputerStatus](https://learn.microsoft.com/en-us/powershell/module/defender/get-mpcomputerstatus)); (12) checked in a real `ad-group-map.example.json` template (Domain Admins / Server Admins / Workstation Administration / Network Engineers mappings, with a `_comment` clarifying it isn't auto-read by the scripts) and updated Appendix A's prose to point at this actual file instead of a nonexistent `ad-group-map.json` — a judgment call to create a concrete example rather than just fixing the reference text, not separately confirmed with the user. Re-validated: PowerShell parser check (`ParseFile`) on all three touched `.ps1` files; a `python3 json.load` check on the new JSON template; a full `-SampleMode` dry run (41 items across 2 drives) with the report auto-rendering and digest verification reporting OK; grepped the rendered HTML confirming each of the 12 fixes' expected/absent text; a full 23-page PDF visual QA pass (`soffice`/`pdftoppm`) confirming no text overflow, wrapping, or truncation issues anywhere in the report; and an isolated unit test of `Resolve-DiskMake` (Samsung/WDC/Seagate prefixes correctly inferred, unknown vendor falls back correctly, real manufacturer strings pass through unchanged). Digest changed from the prior known-good `c37efa0af75bde7330703b556febc6e0b472548c313cbf8e0df8faf72cd58237` to a new value, expected given the content-affecting changes in items (2), (3), (4), (7), (9), and (11) above. Two items flagged rather than silently assumed: the DCN-prompt fix (5) is defensive hardening only, since the reported failure could not be reproduced without a live Windows host; and the Examiner Initials interpretation (6) as a single launch-time prompt (vs. 41 individual prompts) is this implementation's own reading of the request. Updated `README.md` (10 edits: file-table filename references, new `ad-group-map.example.json` row, run-order section, sample transcript, `-ExaminerInitials` examples, regeneration/tamper-evidence sections, design-constraints section) and `manifest.txt` (new file row, renamed SAMPLE evidence filename).
- 2026-08-06 — Removed the `placeholder="---"` hint text from the Examiner Initials field added earlier the same day. With the placeholder present, an untouched field still rendered visible dashes on-screen and in print, which isn't appropriate for a field meant to double as a blank wet-signature line — an examiner filling out a printed copy by hand needs a genuinely blank underline, not one pre-filled with placeholder characters. The field still accepts typed initials when filled in digitally; it's simply blank (a plain underline) when left untouched, on-screen or printed. Widened the input slightly (76px -> 90px) to give a hand-signed initials a bit more room. Re-validated: PowerShell parser check, `-SampleMode` regeneration confirming the items-array digest is unchanged (`c37efa0af75bde7330703b556febc6e0b472548c313cbf8e0df8faf72cd58237`), and a Playwright visual check confirming the field now renders as a clean blank line.
- 2026-08-06 — Fixed the actual root cause of the recurring "it's not generating a report" complaints in the ISV toolchain, plus decoupled the BIOS reference guides and added a per-item sign-off field. Three changes: (1) **`New-ISVReport.ps1` no longer hardcodes the main report's output filename to `Initial-System-Validation-Report-SAMPLE.html`.** That name was written unconditionally on every run, live or sample, so a real live-host run with genuine 41-item evidence produced a file that visually looked like unrendered placeholder/demo output — this is almost certainly what was actually happening on every prior "no report generated" report in this thread, even though the file was, in fact, being written the whole time. The filename is now derived from the evidence itself: `Invoke-ISVCollection.ps1` now records `meta.is_sample_mode` (`true`/`false`) in `evidence.json`; the renderer reads it (defensively, via a `PSObject.Properties.Name -contains` check, so older `evidence.json` files from before this field existed still render instead of throwing under `Set-StrictMode` — confirmed by testing PSCustomObject missing-property access directly, which does throw, same as the existing hashtable case) and names the file `Initial-System-Validation-Report-<hostname>.html` for a real/live bundle, or keeps `Initial-System-Validation-Report-SAMPLE.html` only when `is_sample_mode` is true or absent-and-otherwise-unrecoverable. A new `-ReportFileName` parameter allows an explicit override. (2) **The Dell/HP BIOS reference guides are no longer regenerated by the main renderer on every collection run.** They are generic, host-independent vendor documentation that never changes based on a specific host's evidence, but the old `New-ISVReport.ps1` rewrote both files into the per-host output folder every single time regardless. Split all BIOS-guide-specific code (`Format-BiosItemCard`, `Get-BiosSources`, `New-BiosGuideHtml`, the shared `$GuideStyle` CSS, etc.) out into a new standalone `New-BiosReferenceGuides.ps1`, run manually whenever needed rather than on every audit. Verified byte-for-byte identical HTML output from the new standalone script versus the previously committed guides — zero content drift from the split. (3) **Added an "Examiner Initials" field to every one of the 41 rendered checklist item cards** in the main report, giving a human reviewer a place to sign off on each specific line item after checking its captured evidence, independent of the machine-recorded PASS/FAIL/MANUAL result. Re-validated: PowerShell parser check on all three touched/added files; a `-SampleMode` run confirming the report keeps its `-SAMPLE.html` name and the items-array digest is unchanged (`c37efa0af75bde7330703b556febc6e0b472548c313cbf8e0df8faf72cd58237`, confirming zero drift in the 41 checklist items from this change); a simulated live-mode bundle (`is_sample_mode: false`, real hostname) confirming the report is correctly named after the hostname; a simulated legacy bundle with `is_sample_mode` entirely absent confirming old evidence still renders under the safe hostname-based fallback instead of crashing; a `-ReportFileName` override test; a standalone run of `New-BiosReferenceGuides.ps1` confirming both guides still render, byte-identical to the previously committed versions; and a Playwright/Chromium visual QA pass confirming the new Examiner Initials field renders cleanly with no layout regressions across the report. Updated `README.md` (new filename behavior, `-ReportFileName`, new "Generating the BIOS reference guides" section) and `manifest.txt` (new `New-BiosReferenceGuides.ps1` row).
- 2026-08-06 — Fixed a report-generation gap plus a DCN-modeling error in the ISV toolchain, reported after a live-host run still didn't produce an HTML file. Three changes: (1) **`Invoke-ISVCollection.ps1` now auto-chains into `New-ISVReport.ps1`** at the end of a successful run (opt out with the new `-SkipReport` switch) — this removes the entire "forgot to run the second command" failure mode structurally, regardless of whether that was the original cause (the original root cause on the user's specific host was never directly reproduced/confirmed, since only a non-Windows sandbox was available for testing; this fix eliminates the failure class either way). (2) **DCN is now modeled as per-drive, not system-wide** — `IsvDefinitions.ps1`'s old single `DCN` identification field is replaced by a `$Script:DriveFields` schema (Index/Make/Model/Capacity/Serial/Dcn) and a new `Get-DiskInventory` function (`Win32_DiskDrive` in live mode, two canned drives in `-SampleMode`); `-Dcn` is now `[string[]]`, mapped positionally to detected drives, and the rendered report gained a new "Hard Drives (Drive Control Numbers -- DCN)" table between System Identification and Summary. (3) **Manual prompting moved to after the automated run** (drive detection + all 41 checks first, then the 6 remaining identification fields, then one DCN prompt per detected drive last) specifically so the drive count is known before deciding how many DCN prompts to ask; a host with zero detected drives (e.g. CIM unavailable) falls back to one generic DCN prompt and a single synthetic drive row. Caught two real bugs during this pass's own verification, both fixed before shipping: (a) `Format-DrivesTable` used `$value -eq ''` to detect missing fields, but PowerShell's `-eq` coerces `''` to the left operand's type, so integer `0` (a perfectly valid drive Index) compared equal to `''` and got wrongly replaced with an "N/A" placeholder — fixed by only doing the empty-string check when the value is already a `[string]`; (b) `Get-DiskInventory`'s `return @()` (zero-drive case) and effectively any single-element return collapse to `$null`/a bare scalar at the call site due to PowerShell's pipeline output-unrolling, which would have thrown `The property 'Count' cannot be found` under `Set-StrictMode` — fixed by wrapping the call site as `$disks = @(Get-DiskInventory)`. Re-validated: PowerShell parser check on all three edited files; a `-SampleMode` run confirming the 2-canned-drive table renders correctly (items-array digest unchanged at `c37efa0af75bde7330703b556febc6e0b472548c313cbf8e0df8faf72cd58237`, confirming zero drift in the 41 checklist items despite the meta/schema restructuring); an interactive live-mode run on a 0-detected-drive host confirming the single generic-DCN fallback path; a mocked 3-physical-drive Windows scenario (fake `Get-CimInstance`) confirming three distinct per-drive DCN prompts fire in the correct order and populate the correct rows; a mixed test confirming the `-Dcn` array parameter pre-fills specific drives by position while still prompting only for the remainder; and a Playwright/Chromium visual QA pass on both the sample and mocked 3-drive reports confirming the new Hard Drives table renders with correct column widths and no overflow.
- 2026-08-06 — Fixed two issues in `Invoke-ISVCollection.ps1` found during a real live-host test run: (1) the 7 site-specific identification fields (Inspector, Area/Location, Program, Asset Tag, SCAP Score, DCN, Prior Win10 Upgrade) are now prompted for interactively via `Read-Host` whenever not supplied as a parameter, instead of silently defaulting to `SAMPLE placeholder` text in a live run — parameters still work and are preferred over the prompt when supplied, so unattended/scripted use is unaffected; (2) native command invocations (`reg query`, `icacls`) now redirect STDERR into the captured evidence (`2>&1` at the per-item invocation point) instead of letting it print directly to the console — on Windows, a missing registry key or path previously wrote messages like `ERROR: The system was unable to find the specified registry key or value.` straight to the terminal (bypassing the try/catch, since native-EXE stderr does not respect `$ErrorActionPreference` the way cmdlet errors do), which looked like a crash even though the run completed and the item was already correctly scored; that text now lands cleanly in the item's own evidence field. Root cause confirmed by reproducing the exact stderr-passthrough behavior with an equivalent native command locally before and after the fix. Also clarified in the README that the collector and renderer are two separate steps — a user who only ran `Invoke-ISVCollection.ps1` and didn't see an HTML file had not yet run `New-ISVReport.ps1`, which was the actual cause of "it did not generate a report" (the collector itself ran to completion and wrote `evidence.json` correctly). Re-validated: PowerShell parser check, an interactive run with piped answers confirming clean STDERR and correct evidence capture, sample-mode regeneration confirming the digest is unchanged (`c37efa0af75bde7330703b556febc6e0b472548c313cbf8e0df8faf72cd58237`) so this is a pure bug fix with no behavioral drift in sample output, and a visual re-check of the rendered HTML report.
- 2026-08-06 — Added the Initial System Validation (ISV) audit toolchain at `execution-plan/initial-system-validation/`: a PowerShell-only collector (`Invoke-ISVCollection.ps1`) that executes all 41 checklist items against a live Windows 11 endpoint (or `-SampleMode` for a placeholder dry run) and writes a SHA-256 tamper-evidence-digested `evidence.json`, plus a PowerShell renderer (`New-ISVReport.ps1`) that turns that evidence into the main HTML report and the two vendor BIOS reference guides (Dell/HP). This replaces an earlier Python-based version of the same tooling that was drafted locally but never committed to this repo; the deliverable is PowerShell end-to-end per explicit instruction, not Python. Shared item/BIOS-reference definitions live in `IsvDefinitions.ps1`, dot-sourced by both scripts. Digest is computed over the evidence items array only (never the meta block) using a hand-rolled canonical-JSON serializer matching Python's `json.dumps(sort_keys=True, separators=(",",":"))` behavior, so the format is independently re-verifiable by any tool that can canonicalize JSON the same way. Smoke-tested: sample-mode and live-mode (on a non-Windows host, confirming every check safely degrades to `N/A` with captured error text rather than crashing) collection runs, full HTML rendering of all three deliverables, and a deliberate tamper test confirming the digest-mismatch warning banner fires correctly. Rendered output visually QA'd via headless Chromium. The original Python prototype remains, untouched and outside git, in the separate local `initial-system-validation/` project directory used for early drafting; it is not part of this repo. See `execution-plan/initial-system-validation/README.md` for usage.
- 2026-07-29 — Added `stig_reference_builder.py fetch-compilation`: auto-discovers and downloads the current quarterly DISA SRG-STIG Library Compilation directly into `stig_intake/`, no browser or manual download needed. The per-product download page (`public.cyber.mil/stigs/downloads`) is still correctly documented as unsafe to script (Salesforce Lightning/shadow-DOM, no stable per-product URL) — but the quarterly *compilation* `.zip` turned out to be hosted separately on `dl.dod.cyber.mil` as a plain, unauthenticated static file with a predictable `U_SRG-STIG_Library_<Month>_<Year>.zip` naming pattern, confirmed live via direct HEAD requests. The new command walks backward from the current month (default up to 6, `--max-months-back` to adjust) until it finds a published release, verifies it with a HEAD request before downloading anything, then streams it to disk. `start_here.py`'s Step 3 now offers this automatically when the intake folder is empty (declines by default on Enter/EOF, so it never triggers an unwanted ~350-400 MB download in an unattended or piped run). End-to-end smoke-tested for real: downloaded the live July 2026 compilation (352 MB, ~6s), ran `build` against it (385 XCCDF benchmarks parsed, 19,277 unique finding IDs, including a byte-for-byte match on the existing sample record V-253259/WN11-00-000030), then discarded all test artifacts and reverted the committed `stig_reference.json` back to its original 256-finding sample state. Updated `execution-plan/tools/README.md` (new Option A + files table row) and `execution-plan/README.md` (Step 1 quick-start snippet).
- 2026-07-29 — Added an interactive setup wizard, `execution-plan/tools/start_here.py` (with a thin root-level `start_here.py` wrapper so `python3 start_here.py` works immediately from a fresh clone). Six steps: environment check (Python version, network reachability to NVD/Tenable, cached-entry counts for all three reference DBs), optional recording of who holds each of the 17 JSIG §1.5 roles (saved to a new local-only, gitignored `data/role_assignments.local.json` — never committed, per the repo's unclassified/general-framework scope), guided STIG import, guided CVE pre-fetch, guided Nessus Plugin ID pre-fetch, and a practice Variance/Risk-Acceptance record generation. Shells out to the existing four CLI tools rather than reimplementing logic (prints each command before running it); never edits `MAINTENANCE-PLAN.md` and never commits to git. `--check-only` and per-step `--skip-*` flags supported. Smoke-tested: `--check-only`, full interactive run with piped/empty stdin (confirmed EOF-safe graceful degradation to defaults, no crash), all `--skip-*` flags together, `--help`, and a mid-flow `Ctrl-C` (confirmed clean exit 130, no partial writes) — all left the repo clean, no leftover artifacts. Along the way, fixed a real bug the wizard's environment check surfaced: a bare-root reachability probe to NVD returned HTTP 403 (blocked without a User-Agent) even though the actual CVE API endpoint works fine, so the check now treats any HTTP response (even an error status) as "network path is up" and only flags true connection-level failures as unreachable. Wired into docs: `execution-plan/tools/README.md` (new Quickstart wizard section + files table row), `execution-plan/README.md` (new "Step 0" quick-start entry, folder map, and a Nessus Plugin ID mention added to the previously-stale Section 3 auto-detect description), and root `README.md` (new "0." quickstart pointer). Also corrected two pre-existing stale example commands found in `execution-plan/README.md`'s quick-start workflows while touching that section: `stig_reference_builder.py` was missing its required `build` subcommand, and `cve_reference_builder.py fetch-list` used a nonexistent `--input` flag instead of the real `--file` flag — both re-verified against actual `--help` output and a live run.
- 2026-07-29 — Added `nessus_reference_builder.py` and extended `generate_variance.py` to auto-detect and render a third variance-record identifier type: bare-digit Nessus Plugin IDs (e.g. `156327`), alongside the existing STIG `V-NNNNNN` and CVE `CVE-YYYY-NNNN` paths. Fetches official plugin metadata from Tenable's public plugin pages (no auth/API key needed). Added `nessus_intake/` drop zone and updated `execution-plan/tools/README.md`. Smoke-tested end-to-end same day: all three ID-type paths (STIG V-253254, CVE CVE-2021-44228, Nessus Plugin 156327) generated valid Markdown/HTML records; `build_control_title_index.py`, `build_role_task_index.py`, and `build_raci_matrix.py` re-run and confirmed byte-identical to committed output (no drift). Test artifacts discarded per the established no-test-data-in-repo convention.
- 2026-07-17 — Repository created: JSIG reference scaffold, cadence research, maintenance plan (daily-ops-focused with deep-dive domain analysis), 17 role playbooks, and full offline external-source library assembled.
- 2026-07-17 — Full-scope re-extraction: obtained complete 378-page JSIG PDF via Wayback Machine snapshot and `pdftotext -layout`; regenerated all 18 Chapter 3 families, all 8 Privacy sub-families, and Appendices A/B/D/E with real JSIG-specific text (ODP values, DoD SAP-specific supplemental guidance); built a dedicated verified parser for the full 963-row Appendix C baseline table, replacing the prior partial extraction. See `reference/JSIG-source/EXTRACTION-LOG.md` for full method and known limitations. ODP reconciliation against `MAINTENANCE-PLAN.md` not yet started (see Open questions).
- 2026-07-17 — RACI/runbook control-language reconciliation (framework, not calendar): built `control_title_index.json` (949 real verbatim JSIG control titles parsed from the extracted family files) and `CONTROL-LANGUAGE-CROSSWALK.md` (audits all 127 distinct Control IDs cited in the Master Calendar — 0 mismatches found). Enriched `role_task_index.json` with resolved real titles per task (additive `control_titles` field; RACI counts per role unchanged, confirming no logic drift) and added a `CONTROL-LANGUAGE-CROSSWALK.md` pointer to `RACI-MATRIX.md`'s header and all 17 role runbooks. `MAINTENANCE-PLAN.md`'s 110-task calendar itself was intentionally left untouched per explicit scoping — this pass verified the framework's existing Control ID citations against real JSIG text rather than reworking the calendar's task/frequency structure.
